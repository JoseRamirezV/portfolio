---
import ProjectCard from '@/components/ProjectCard.astro';
const { projects } = Astro.props
---

<div class="flex flex-col mb-10 gap-y-16">
    {projects.map(project => (
        <ProjectCard {...project}/>
    ))}
</div>